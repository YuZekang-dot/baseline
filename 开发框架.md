# 腾讯开悟强化学习开发框架说明

本文档根据新的 `新建 Microsoft Word 文档.docx` 整理而成，面向腾讯开悟强化学习开发框架的代码包结构、环境交互、智能体开发、训练工作流与 KaiwuDRL 分布式计算框架。原 Word 中重复出现的 API 参数表、接口说明和训练流程表已合并，粘连代码已整理为 Markdown 代码块。

## 综述

腾讯开悟强化学习开发框架是基于强化学习系统系列技术标准打造的标准化开发套件。该框架为开发者提供标准化编程接口和工具集，支持开发者完成智能体开发、环境交互、模型训练和模型预测流程。

框架主要包含三大核心模块：

- 强化学习环境系统：提供标准强化学习环境接口，支持智能体与环境交互。
- 强化学习智能体开发套件：提供标准智能体接口，以及算法库、模型组件库等工具函数库。
- 强化学习分布式计算框架：提供训练工作流接口，支持单机或分布式训练及评估任务。

## 训练流程简介

本开发框架的完整训练流程如下图所示：

![训练流程图](assets/开发框架/image1.png)

完整训练流程包含以下关键环节：

| 环节 | 介绍 |
| --- | --- |
| 智能体与环境循环交互 | 智能体将环境观测和奖励处理为预测函数输入，调用预测函数生成动作指令，再将动作处理为环境可接收的数据；环境执行动作后完成状态转移，并反馈新的观测和奖励。 |
| 样本处理 | 智能体与环境从开始到结束的完整交互过程称为 episode；每次交互产生的结构化数据称为样本；一个 episode 的样本序列称为轨迹；轨迹会被转换为规范化训练样本 `SampleData`。 |
| 模型迭代优化 | 基于训练样本，通过算法持续更新模型参数，实现策略优化。 |
| 智能体模型更新 | 智能体加载最新模型，与环境继续循环交互。 |

## 代码包结构

开发者可以通过腾讯开悟平台提供的强化学习项目使用开发框架。标准代码包目录通常如下，当前比赛代码包也可能按算法拆分为 `agent_ppo/`、`agent_diy/` 等智能体目录。

```text
根目录
├── agent/
│   ├── algorithm/
│   │   ├── __init__.py
│   │   └── algorithm.py
│   ├── conf/
│   │   ├── __init__.py
│   │   ├── conf.py
│   │   └── train_env_conf.toml
│   ├── feature/
│   │   ├── __init__.py
│   │   ├── definition.py
│   │   └── preprocessor.py
│   ├── model/
│   │   ├── __init__.py
│   │   └── model.py
│   ├── workflow/
│   │   ├── __init__.py
│   │   └── train_workflow.py
│   ├── __init__.py
│   └── agent.py
├── conf/
│   ├── __init__.py
│   └── configure_app.toml
├── log/
└── train_test.py
```

### 根目录说明

| 目录或文件 | 介绍 |
| --- | --- |
| `agent/` | 智能体子目录，智能体相关内容集中于该目录，是开发者核心工作目录。 |
| `conf/` | 配置文件目录，包含运行训练任务相关的配置，例如样本批处理大小 `batch_size`。 |
| `log/` | 日志目录，存放运行代码测试脚本时生成的日志文件。 |
| `train_test.py` | 代码正确性测试脚本，会使用当前代码包完成一步训练。建议开发者启动训练任务前先通过该脚本检测。 |

### 智能体目录说明

| 目录或文件 | 介绍 |
| --- | --- |
| `algorithm/` | 算法相关代码，开发者在该目录完成 loss 计算、模型优化等实现。 |
| `feature/` | 特征相关代码，开发者在该目录完成数据结构定义、数据处理、样本处理和奖励计算。 |
| `model/` | 模型相关代码，开发者在该目录完成神经网络模型实现。 |
| `workflow/` | 工作流目录，开发者在该目录完成训练工作流开发。 |
| `agent.py` | 智能体核心代码文件，开发者在该文件完成预测、训练、模型保存和加载等核心函数。 |

### 配置目录说明

| 文件名 | 介绍 |
| --- | --- |
| `configure_app.toml` | 训练任务相关配置，包括样本池容量、训练批大小、模型保存频率、预训练模型等。 |

## 强化学习环境系统

强化学习环境是基于输入动作输出观测、奖励等反馈的功能模块，用于表达强化学习算法要求解的问题场景。开发框架通过场景适配模块封装仿真器，将特化接口和协议转换为强化学习环境统一接口，供智能体调用。

环境系统主要提供以下能力：

- 接收配置信息，用于指定初始化方式，例如环境中各类元素的初始状态。
- 输出观测和奖励信息，用于智能体预测与训练。
- 输出观测、奖励之外的其他信息，例如可视化数据、日志数据、监控数据等。
- 接收动作指令，完成状态转移并产生新的观测和奖励。

由于不同环境的数据协议存在差异，`obs`、`state`、`score`、`act` 的具体结构应以对应环境的数据协议文档为准。

### `reset(usr_conf)`

`reset()` 会将环境重置为配置指定的状态，并返回初始观测。

```python
# usr_conf 为开发者传入的环境配置
obs, state = env.reset(usr_conf=usr_conf)
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| `usr_conf` | `dict` 类型，环境配置文件。 |

返回值：

| 参数名 | 介绍 |
| --- | --- |
| `obs` | `dict` 类型，环境观测信息。 |
| `state` | `dict` 类型，环境全局信息。 |

### `step(act, stop_game=False)`

环境会执行传入的 `act` 动作指令，完成一次状态转移，并返回新的观测、奖励和结束信息。

```python
frame_no, _obs, score, terminated, truncated, _state = env.step(
    act,
    stop_game=False,
)
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| `act` | `dict` 类型，环境执行的动作。 |
| `stop_game` | `bool` 类型，是否结束当前对局。 |

返回值：

| 参数名 | 介绍 |
| --- | --- |
| `frame_no` | `int` 类型，当前环境实例运行时的帧号。 |
| `_obs` | `dict` 类型，当前帧的观测信息。 |
| `score` | `int` 或数值类型，当前帧的环境反馈分数或奖励信息。 |
| `terminated` | `bool` 类型，当前环境实例是否结束。 |
| `truncated` | `bool` 类型，当前环境实例是否异常或中断。 |
| `_state` | `dict` 类型，当前帧的全部状态信息。 |

## 强化学习智能体开发套件

智能体是强化学习系统中的核心模块。围绕完整训练流程，智能体开发通常分为四部分：

- 数据处理与奖励设计：基于环境观测数据完成特征处理、样本处理和奖励设计。
- 模型开发：实现神经网络模型。
- 算法开发：实现算法优化过程。
- 工作流开发：实现自定义训练工作流。

### 数据结构定义

开发目录：`<智能体文件夹>/feature/definition.py`

开发框架预先约定三类基础数据结构：`ObsData`、`ActData`、`SampleData`。

- `ObsData`：智能体预测输入，由 `agent.predict()` 使用。
- `ActData`：智能体预测输出，由 `agent.action_process()` 转换为环境动作。
- `SampleData`：训练样本类型，由 `agent.learn()` 使用。

`create_cls()` 用于动态创建数据结构。属性名称和属性数量由开发者自定义。

```python
ObsData = create_cls("ObsData", feature=None)
ActData = create_cls("ActData", action=None, prob=None)
SampleData = create_cls("SampleData", npdata=None)
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| 第一个参数 | 字符串类型，类的名称。 |
| 其余参数 | 类的属性，默认值为 `None`，由开发者自行定义。 |

### 观测处理

开发目录：`<智能体文件夹>/agent.py`

环境的 `reset()` 和 `step()` 返回原始观测数据，通常无法直接作为智能体预测输入。开发者需要将原始观测特征化，转换为 `ObsData`。

```python
def observation_process(self, obs, state=None):
    return ObsData(feature=feature, legal_act=legal_actions)
```

建议将大量数值处理、数据转换和领域特征工程放在 `<智能体文件夹>/feature/preprocessor.py` 中，再由 `observation_process()` 调用。

参数：

| 参数名 | 介绍 |
| --- | --- |
| `obs` | `Observation` 类型，`env.reset()` 和 `env.step()` 返回的环境观测数据。 |
| `state` | `EnvInfo` 类型，`env.reset()` 和 `env.step()` 返回的环境状态数据。 |

返回值：

| 参数名 | 介绍 |
| --- | --- |
| `ObsData` | 开发者定义的 `ObsData` 类型数据，将作为 `agent.predict()` 的输入。 |

### 动作处理

开发目录：`<智能体文件夹>/agent.py`

环境 `step()` 的输入必须满足环境特定数据协议，开发者需要将智能体输出的 `ActData` 转换为环境可以接收的动作数据。

```python
def action_process(self, act_data):
    return act_data.act
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| `act_data` | 开发者定义的 `ActData` 类型数据。 |

返回值为环境可处理的动作数据，作为 `env.step()` 的输入。

### 奖励设计

开发目录：`<智能体名称>/feature/definition.py`

这里的奖励指强化学习中的 Reward，需要与环境反馈的 Score 区分。Score 通常用于衡量智能体在环境中的实际表现；Reward 则用于算法训练，开发者可以结合环境观测、状态、经验和领域知识自行设计。

```python
def reward_shaping(obs, _obs, state, _state):
    return reward
```

参数个数和类型不限制，可以是环境信息、智能体信息、开发者经验和知识等。返回值为数值类型，即计算出的 reward。

### 样本处理

开发目录：`<智能体文件夹>/feature/definition.py`

智能体与环境交互产生的轨迹数据通常无法直接作为训练输入，需要转换为开发者定义的 `SampleData`。

```python
@attached
def sample_process(self, list_game_data):
    return [SampleData(**i.__dict__) for i in list_game_data]
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| `list_game_data` | `list[Frame]` 类型，使用开发者自定义的 `Frame` 作为输入；样本通常批处理，因此传入列表。 |

返回值：

| 参数名 | 介绍 |
| --- | --- |
| `list[SampleData]` | 由 `SampleData` 类型数据组成的列表。 |

为了支持分布式训练，样本数据需要进行网络传输。由于 `SampleData` 无法直接网络传输，需要先转换为 `numpy.ndarray`，传输到对端后再转换回 `SampleData`。这两个转换函数互为反函数，且必须包含 `@attached` 装饰器。

```python
@attached
def SampleData2NumpyData(g_data):
    return g_data.npdata


@attached
def NumpyData2SampleData(s_data):
    return SampleData(npdata=s_data)
```

参数：

| 函数 | 参数 | 介绍 | 返回值 |
| --- | --- | --- | --- |
| `SampleData2NumpyData` | `g_data` | `SampleData` 类型。 | `numpy.ndarray` 类型。 |
| `NumpyData2SampleData` | `s_data` | `numpy.ndarray` 类型。 | `SampleData` 类型。 |

## 模型开发

开发目录：`<智能体名称>/model/model.py`

强化学习模型是基于特征作为输入、输出策略的神经网络模型。开发者需要在 `model.py` 中实现神经网络模型。开发框架要求模型类继承 `torch.nn.Module`，即符合 PyTorch 模型实现规范。

```python
import torch.nn as nn


class Model(nn.Module):
    def __init__(self, state_shape, action_shape=0, softmax=False):
        super().__init__()
        # Define network layers here.
```

## 算法开发

开发目录：`<智能体名称>/algorithm/algorithm.py`

完成特征处理和奖励设计后，开发者需要实现强化学习算法，通过特定优化方法更新模型参数。

### `Algorithm.learn(list_sample_data)`

`learn()` 是实现强化学习优化算法的核心方法。该函数输入训练样本数据，开发者需基于具体算法完成优化方法、损失计算和参数更新。

```python
def learn(self, list_sample_data):
    """Implement the core method of the algorithm."""
    loss = 0

    # Calculate loss based on the selected algorithm.
    loss.backward()

    # Update weights with gradient descent or another optimizer.
    self.optimizer.step()
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| `list_sample_data` | `list` 类型，训练样本 `SampleData` 列表。 |

## 智能体核心接口

开发目录：`<智能体名称>/agent.py`

完成模型和算法后，开发者需要实现强化学习智能体。智能体使用模型进行决策，与环境交互，并通过算法更新模型参数。

### `learn(list_sample_data)`

该函数输入训练样本数据，开发者需要在函数中调用算法消费训练样本并进行训练。

- 单机训练：开发者需要在训练工作流中手动调用该函数进行一步训练。
- 分布式训练：该函数作为训练函数会被循环执行；同时它也可作为样本发送函数，由训练工作流手动调用，将样本发送至样本池。

```python
def learn(self, list_sample_data):
    self.algo.learn(list_sample_data)
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| `list_sample_data` | `list` 类型，训练样本 `SampleData` 列表。 |

### `predict(list_obs_data, list_state)`

`predict()` 通过调用模型进行预测，通常在训练时调用。训练阶段一般会依策略概率分布采样，或引入随机探索。

```python
@predict_wrapper
def predict(self, list_obs_data, list_state):
    return [ActData]
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| `list_obs_data` | `list` 类型，观测数据 `ObsData` 列表。 |
| `list_state` | 可选参数，`list` 类型，环境返回的状态数据列表。 |

返回值：

| 参数名 | 介绍 |
| --- | --- |
| `list[ActData]` | 开发者定义的动作数据 `ActData` 列表。 |

### `exploit(observation)`

`exploit()` 通过调用模型进行预测，通常在评估时调用。评估阶段一般选取策略中概率最高的动作，或策略认为最优的动作。

```python
@exploit_wrapper
def exploit(self, observation):
    return action
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| `observation` | `dict` 类型，环境观测字典；评估工作流会将原始环境观测传入 `agent.exploit()`。 |

返回值：

| 参数名 | 介绍 |
| --- | --- |
| `action` | `list` 类型，环境可以直接使用的动作指令。 |

### `load_model(path=None, id="1")`

智能体通过该接口完成模型参数加载。分布式训练中，Actor 会从模型池获取最新模型参数文件，开发者需要手动调用 `load_model()` 让智能体完成参数加载。

```python
@load_model_wrapper
def load_model(self, path=None, id="1"):
    # Loading multiple files is supported.
    # Keep every filename consistent with save_model().
    model_file_path = f"{path}/model.ckpt-{str(id)}.pkl"
    self.model.load_state_dict(
        torch.load(model_file_path, map_location=self.device),
    )
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| `path` | `string` 类型，模型参数文件路径，由开发框架根据使用场景传入。 |
| `id` | `string` 类型，模型参数文件 ID，用于指定加载哪个模型参数文件。 |

### `save_model(path=None, id="1")`

开发者可以通过该函数保存当前时刻的模型文件及智能体代码包。开发框架会将需要保存的内容打包为 zip 文件。

- 使用腾讯开悟客户端开发时，框架会在客户端指定目录下存储 zip 文件。
- 使用腾讯开悟平台时，框架会将 zip 文件存储在云端，开发者可在训练管理模块查看每个训练任务的模型文件。

```python
@save_model_wrapper
def save_model(self, path=None, id="1"):
    # The filename must include the "model.ckpt-id" field.
    model_file_path = f"{path}/model.ckpt-{str(id)}.pkl"

    # Copy model state dict to CPU before saving.
    model_state_dict_cpu = {
        k: v.clone().cpu()
        for k, v in self.model.state_dict().items()
    }
    torch.save(model_state_dict_cpu, model_file_path)
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| `path` | `string` 类型，模型文件保存路径，由开发框架根据使用场景传入。 |
| `id` | `string` 类型，模型文件索引，开发框架获取模型池中最新模型索引后传入。 |

## 工作流开发

开发目录：`<智能体名称>/workflow/train_workflow.py`

完成智能体开发后，需要实现由分布式计算框架提供的训练工作流接口，使智能体和环境持续交互，收集训练样本，迭代模型参数，最终完成策略优化。

### `workflow(envs, agents, logger=None, monitor=None)`

`workflow()` 是训练工作流核心函数。开发者通过该函数调用智能体和环境接口，完成环境交互、样本收集和模型更新。

```python
@attached
def workflow(envs, agents, logger=None, monitor=None):
    pass
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| `envs` | `list` 类型，环境列表，返回当前正在运行的环境。 |
| `agents` | `list` 类型，智能体列表，由 `<智能体名称>/agent.py` 实例化 `Agent` 后传入。 |
| `logger` | `Logger` 类型，框架提供的日志组件，接口与 Python `logging` 库一致。 |
| `monitor` | `Monitor` 类型，框架提供的监控组件。 |

### 训练工作流示例

以下示例展示训练工作流的关键步骤，具体实现由开发者按项目补齐。

```python
@attached
def workflow(envs, agents, logger=None, monitor=None):
    # Get the environment and agent.
    env, agent = envs[0], agents[0]

    epoch_num = 1000
    episode_num_every_epoch = 1000
    last_save_model_time = time.time()

    for epoch in range(epoch_num):
        for g_data in run_episodes(
            episode_num_every_epoch,
            env,
            agent,
            logger,
            monitor,
        ):
            # Stand-alone training trains directly.
            # Distributed training sends samples to the sample pool.
            agent.learn(g_data)
            g_data.clear()

        now = time.time()
        if now - last_save_model_time >= 300:
            agent.save_model()
            last_save_model_time = now


def run_episodes(n_episode, env, agent, logger, monitor):
    for episode in range(n_episode):
        done = False
        collector = []

        obs, state = env.reset(usr_conf=usr_conf)
        obs_data = agent.observation_process(obs, state)

        # Load the latest model on demand.
        # Stand-alone training may skip remote model loading.
        agent.load_model(id="latest")

        while not done:
            act_data = agent.predict(list_obs_data=[obs_data])[0]
            act = agent.action_process(act_data)

            frame_no, _obs, score, terminated, truncated, _state = env.step(act)
            if _obs is None:
                break

            _obs_data = agent.observation_process(_obs, _state)

            if truncated and frame_no is None:
                break

            reward = reward_shaping(obs_data, _obs_data, state, _state)
            done = terminated or truncated

            frame = Frame(
                obs=obs_data.feature,
                _obs=_obs_data.feature,
                act=act,
                rew=reward,
                done=done,
            )
            collector.append(frame)

            if done:
                if len(collector) > 0:
                    collector = sample_process(collector)
                yield collector
                break

            obs_data = _obs_data
            obs = _obs
            state = _state
```

## KaiwuDRL 分布式计算框架

在强化学习项目开发中，分布式计算框架是支撑大规模训练任务的核心基础设施。开发框架提供由腾讯王者荣耀团队自主研发的强化学习分布式计算框架 KaiwuDRL，通过并行化计算、资源调度和分布式协同优化，提升智能体训练效率与稳定性。

### 总体架构

![KaiwuDRL 总体架构图](assets/开发框架/image2.png)

KaiwuDRL 的整体架构包括 Environment、Aisrv、Actor、Learner 等强化学习组件，均支持多实例并行运行。此外，还集成通信、日志、监控、对象存储等基础组件。

| 组件名称 | 功能描述 |
| --- | --- |
| `Environment` | 环境服务组件，负责运行强化学习环境，支持通过标准接口与环境交互，并返回环境观测 `obs`。 |
| `Aisrv` | 训练流程中枢，负责收集环境样本，运行训练、评估工作流，以及处理组件间数据传输。 |
| `Actor` | 预测服务组件，负责响应 Aisrv 的预测请求，调用智能体 `predict()` 或 `exploit()` 生成动作决策结果。 |
| `Learner` | 训练服务组件，负责采集训练样本，调用智能体 `learn()` 完成梯度计算和模型迭代。 |
| `MemoryPool` | 样本存储组件，简称样本池。负责存储训练样本，接收 Aisrv 打包的训练样本，并发往 Learner 用于训练。 |
| `ModelPool` | 模型存储组件，简称模型池。负责存储模型参数文件，接收 Learner 产出的模型参数文件，并将最新参数发送给 Actor。 |
| 日志 | 日志采集组件，负责记录各组件运行日志，支持通过标准接口上报日志。 |
| 监控 | 监控采集组件，负责采集系统资源使用率、训练指标趋势等数据，支持通过标准接口上报指标。 |

`MemoryPool` 和 `ModelPool` 仅在分布式训练时启用。

### 预测服务

预测服务的数据流如下：

1. Aisrv 向 Environment 发送环境配置并创建新 episode。
2. Environment 向 Aisrv 返回原始观测数据。
3. Aisrv 基于原始观测数据向 Actor 发送预测请求。
4. Actor 使用预测请求中的观测进行特征处理，智能体基于特征处理后的数据进行预测，并将预测数据处理为环境可识别的动作指令。
5. Aisrv 使用动作指令与 Environment 交互，Environment 返回新的观测。

### 训练服务

训练服务的数据流如下：

1. Aisrv 在预测服务中不断产生轨迹数据。
2. Aisrv 完成样本处理，并发送至样本池。
3. Learner 从样本池按批采集样本进行训练。
4. Learner 将最新模型参数同步至 Actor。

### 训练与评估工作流

KaiwuDRL 提供训练、评估工作流接口，开发者可以按需调用组件和服务，实现模型训练与评估。

- 训练工作流：开发目录为 `<智能体名称>/workflow/train_workflow.py`，核心函数为 `workflow(envs, agents, logger=None, monitor=None)`。
- 评估工作流：运行训练任务并获得模型文件后，可通过评估任务验证模型能力。使用腾讯开悟平台项目时，评估工作流由官方实现，开发者无法修改；评估工作流会调用开发者自定义的 `agent.exploit()`。

## 分布式训练配置项

开发目录：`/conf/configure_app.toml`

Learner 每次执行 `learn()` 时会从样本池采样一批样本作为输入，并按照 `dump_model_freq` 配置频次保存模型参数文件。模型同步服务会按照 `model_file_sync_per_minutes` 将模型参数文件推送至模型池，Actor 也按该频次从模型池获取最新模型。

```toml
[app]

# 执行 learn() 函数进行训练的时间间隔。
# 可通过该配置让 Learner 休息，以调节样本生产与消费比例。
learner_train_sleep_seconds = 0.001

# 样本池容量。
replay_buffer_capacity = 4096

# 当样本池中的样本占总容量的比例达到该值时，启动训练。
preload_ratio = 1.0

# 新样本加入样本池时，旧样本的移除逻辑。
# 可选项：reverb.selectors.Lifo、reverb.selectors.Fifo。
reverb_remover = "reverb.selectors.Fifo"

# Learner 从样本池采样的逻辑。
# 可选项：reverb.selectors.Fifo、reverb.selectors.Uniform。
reverb_sampler = "reverb.selectors.Uniform"

# Learner 训练时样本批处理大小。
train_batch_size = 2048

# 训练间隔多少步输出模型参数文件。
dump_model_freq = 1000

# Learner 推送模型参数文件至模型池，以及 Actor 从模型池获取模型参数文件的频次，单位为分钟。
model_file_sync_per_minutes = 1

# Learner 每次推送模型参数文件、Actor 每次获取模型参数文件的数量，上限为 50。
modelpool_max_save_model_count = 1
```

## 其他功能

### 加载预训练模型

框架支持在已有模型基础上继续训练。预训练模型结构和继续训练的模型结构需保持一致。

- 使用腾讯开悟平台时，在创建训练任务时可在弹窗中选择预训练模型继续训练。
- 使用腾讯开悟客户端时，需要将训练好的模型文件，即客户端工作空间 `ckpt` 路径下的 `.pkl` 文件，放入代码包，并在 `conf/configure_app.toml` 中完成预训练模型配置。

配置说明：

```toml
# 是否启用加载预训练模型功能。
# true 表示在 preload_model_dir 中加载 preload_model_id 指定的模型作为初始模型。
# false 表示不加载预训练模型。
preload_model = false

# 预训练模型文件夹相对路径。
# {agent_name} 指代码包中的 agent_算法名目录。
# 仅在 preload_model = true 时生效。
preload_model_dir = "{agent_name}/ckpt"

# 预训练模型标识 ID，通常指模型训练步数。
# 该 ID 对应模型文件名中的训练步数记录。
# 仅在 preload_model = true 时生效。
# 注意：禁止修改原始模型文件名，否则预训练模型加载会失败。
preload_model_id = 1000
```

客户端继续训练示例，以加载 40052 步的 DQN 模型为例：

1. 解压模型压缩包，在 `ckpt` 路径下找到 `model.ckpt-40052.pkl`。
2. 在代码包路径下新建 `agent_dqn/ckpt` 目录，并放入上述模型文件。
3. 在 `conf/configure_app.toml` 中配置：

```toml
preload_model = true
preload_model_dir = "agent_dqn/ckpt"
preload_model_id = 40052
```

完成后，Aisrv 和 Learner 将加载预训练模型并继续训练。

### 加载对手模型

使用腾讯开悟平台时，部分项目支持从平台模型管理中加载自定义模型。用户可以在 `kaiwu.json` 中配置期望用作对手模型的模型 ID。智能体通过 `load_opponent_agent()` 完成对手模型加载。

该功能允许智能体加载指定网络结构和参数，并根据模型文件中的实现调用 agent 的 `reset()`、`predict()`、`exploit()` 等方法。对手模型可作为固定水平的自定义模型，在 PVP 任务中用于训练评估。

接口示例：

```python
@load_opponent_agent_wrapper
def load_opponent_agent(self, id="1"):
    # Framework provides loading opponent agent function.
    # No implementation is required here.
    pass
```

参数：

| 参数名 | 介绍 |
| --- | --- |
| `id` | `string` 类型，平台模型管理的模型 ID，用于指定加载的模型文件。 |

`kaiwu.json` 配置示例：

```json
{
  "model_pool": [609, 608]
}
```

### 策略类型选择

框架支持两种核心训练范式：同策略（On-Policy）与异策略（Off-Policy）。该选择会影响数据使用方式、内存占用和部分超参数推荐设置。

| 特性 | On-Policy | Off-Policy |
| --- | --- | --- |
| 数据来源 | 必须使用当前正在训练的策略实时与环境交互产生的新数据。 | 可以使用历史经验数据，这些数据可能来自旧版本策略或不同探索策略。 |
| 数据使用 | 一次性为主。数据在用于一次策略更新后通常被丢弃，以保证训练数据与当前策略一致。 | 可重复利用。数据被存储在经验回放缓冲区中，可被多次采样用于训练。 |
| 主要优势 | 训练稳定，理论收敛性有保障，行为容易理解。 | 数据效率高，可充分利用每一次交互获得的数据，更适合现实世界中交互成本高的场景。 |

在 `conf/configure_app.toml` 中可通过以下参数设置训练范式：

```toml
# 训练范式：on-policy 或 off-policy。
algorithm_on_policy_or_off_policy = "on-policy"

# 模型保存频率，单位为训练步。
# on_policy：建议设置为 1，训练效果最好。
# off_policy：建议设置为 1000，以减少模型保存频率。
dump_model_freq = 1
```

## 参考链接

- [强化学习开发框架综述](https://tencentarena.com/docs/p-competition-drone_obstacle_nav/7.0.2/guidebook/taa-rl-fw/intro/)
- [强化学习环境系统](https://tencentarena.com/docs/p-competition-drone_obstacle_nav/7.0.2/guidebook/taa-rl-fw/rl_env/)
- [智能体开发概述](https://tencentarena.com/docs/p-competition-drone_obstacle_nav/7.0.2/guidebook/taa-rl-fw/rl_agent/info/)
- [数据处理与奖励设计](https://tencentarena.com/docs/p-competition-drone_obstacle_nav/7.0.2/guidebook/taa-rl-fw/rl_agent/feature/)
- [模型开发](https://tencentarena.com/docs/p-competition-drone_obstacle_nav/7.0.2/guidebook/taa-rl-fw/rl_agent/model/)
- [算法开发](https://tencentarena.com/docs/p-competition-drone_obstacle_nav/7.0.2/guidebook/taa-rl-fw/rl_agent/algorithm/)
- [工作流开发](https://tencentarena.com/docs/p-competition-drone_obstacle_nav/7.0.2/guidebook/taa-rl-fw/rl_agent/workflow/)
- [分布式计算框架](https://tencentarena.com/docs/p-competition-drone_obstacle_nav/7.0.2/guidebook/taa-rl-fw/distributed_computing_fw/)
- [强化学习系统技术标准](https://tencentarena.com/docs/p-competition-drone_obstacle_nav/7.0.2/guidebook/taa-rl-fw/rl_standard/)
